server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: note-service
  data:
    mongodb:
      uri: mongodb+srv://medilabo:root@cluster0.pfjuogp.mongodb.net/notedb?retryWrites=true&w=majority&authSource=admin
      auto-index-creation: true

security:
  jwt:
    secret: ${JWT_SECRET:change-me-with-at-least-32-characters}
    expiration-minutes: 120

management:
  endpoints:
    web:
      exposure:
        include: health,info
